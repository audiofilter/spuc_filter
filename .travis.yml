os:
  - osx
language: 
  - objective-c
compiler:
  - clang
before_install:
  - brew update
install:
  - brew install cmake
  - brew install qt5
  - brew linkapps
# Change this to your needs
before_script:
  - git clone https://github.com/audiofilter/spuc.git
  - mkdir build
  - cd build
  - cmake ..
script: make
